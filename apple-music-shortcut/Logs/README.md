# Runtime logs

Create or copy this folder to `iCloud Drive/Shortcuts/WITR Import/Logs`.

The Shortcut creates and appends to:

- `unmatched.txt`
- `invalid-lines.txt`
- `completed-batches.txt`

These logs are runtime output and are not generated by the batch preparation
script.
